# 数据库迁移策略参考

## 规则

- **版本管理**：使用标准化迁移工具（如 Flyway、Liquibase、Alembic）
- **强制双提交**：Schema 变更（DDL） + 实体代码更新必须在同一 PR
- **版本号规则**：全局递增，禁止回退；出错用补偿脚本
- **禁止手动改表**：任何环境（dev/test/prod）禁止直连数据库执行 ALTER，必须通过迁移工具
- **基线脚本**：直接从设计阶段产出的 DDL 生成 V1__baseline
